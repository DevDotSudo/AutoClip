"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function PaymentReviewActions({id}:{id:string}){const router=useRouter();const[note,setNote]=useState("");const[reason,setReason]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function act(action:"approve"|"reject"){if(action==="reject"&&reason.trim().length<3){setError("Choose or enter a rejection reason.");return}if(action==="approve"&&!window.confirm("Approve this payment and activate the customer plan?"))return;setBusy(true);setError("");try{const response=await fetch(`/api/admin/payments/${id}/${action}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({note,reason})});const result=await response.json();if(!response.ok)throw new Error(result.error||`Unable to ${action} payment.`);router.refresh();}catch(cause){setError(cause instanceof Error?cause.message:"Action failed.");setBusy(false)}}return <div className="review-actions"><div className="field"><label htmlFor="admin-note">Internal admin note</label><textarea id="admin-note" rows={3} value={note} onChange={e=>setNote(e.target.value)}/></div><div className="field"><label htmlFor="reason">Rejection reason</label><select id="reason" value={reason} onChange={e=>setReason(e.target.value)}><option value="">Select a reason</option><option>Invalid receipt</option><option>Incorrect amount</option><option>Reference number not found</option><option>Receipt unreadable</option><option>Duplicate payment</option><option>Wrong payment destination</option><option>Other</option></select></div>{error&&<div className="form-error">{error}</div>}<div className="action-row"><button className="btn btn-secondary danger-button" disabled={busy} onClick={()=>act("reject")}>Reject payment</button><button className="btn btn-primary" disabled={busy} onClick={()=>act("approve")}>Approve payment</button></div></div>}
